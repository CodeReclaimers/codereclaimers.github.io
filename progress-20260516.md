# 2026-05-16

## Summary

Added NeatEvolution.jl and Arborist.jl sections to the site homepage, and added Arborist.jl to the machine-readable `llms.txt` projects list.

## Files modified

- `index.html` — new `<h2>` sections for NeatEvolution.jl and Arborist.jl, placed between NEAT-python and Morphe.
- `llms.txt` — appended Arborist.jl entry to the Projects list (NeatEvolution.jl was already present).

## Key decisions

- **Location for the new sections**: chose `index.html` (the visible homepage) over `README.md` after observing that `README.md` is a 3-line placeholder with no project sections at all. The user's request referenced "neat-python and NEATEvolution.jl" as templates — neat-python lives in `index.html`, and NeatEvolution.jl lived in `llms.txt`. After confirming with the user, NeatEvolution.jl was added to `index.html` as well so both Julia projects have visible sections alongside the Python one.
- **Ordering**: NEAT-python → NeatEvolution.jl → Arborist.jl → Morphe → SketchBridge. Groups the three evolution libraries together at the top, then the CAD tools. Did not reorder the existing Morphe/SketchBridge pair.
- **Section content**: pulled descriptions from each repo's README and GitHub description rather than inventing copy. Arborist.jl description highlights the SciML Problem/Algorithm/Solve pattern, NEAT support, NSGA-II, quality-diversity, and LLM-as-operator — the headline capabilities from its README. NeatEvolution.jl description names the four network types (feed-forward, recurrent, CTRNN, Izhikevich spiking) and notes registry availability.

## Verification

- Reviewed diff visually; both new `<h2>` blocks match the surrounding HTML structure (no Jekyll/templating involved).
- No build or test step exists for this site — GitHub Pages serves files directly.

## What was intentionally NOT done

- Did **not** edit `README.md`. The user said "README" but the literal `README.md` is a placeholder; the actual reader-facing equivalents are `index.html` and `llms.txt`.
- Did **not** update the homepage `<meta name="description">` / Open Graph description, which still reads "Creator of NEAT-python, Morphe, and SketchBridge." Out of scope for this request — flagging for a follow-up if the user wants the Julia projects represented in social-card previews and search snippets.
- Did **not** update `sitemap.xml`. These are external project sections, not new site pages, so no new URL entries are warranted. Did not bump the homepage `<lastmod>` either — keeping scope tight; can be revisited if SEO freshness matters.
- Did **not** post to the Notice Board (per user instruction this session).

## Next steps

- Optional: update `index.html` meta description / Open Graph copy to mention the Julia projects.
- Optional: bump homepage `<lastmod>` in `sitemap.xml` if we want search engines to recrawl sooner.
